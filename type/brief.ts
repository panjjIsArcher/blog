export interface Describution {
    describution: string,
    id: number
}

export interface Media {
    app: string,
    link: string,
    icon: string
}

export interface Stack {
    name: string,
    skills: string[]
}